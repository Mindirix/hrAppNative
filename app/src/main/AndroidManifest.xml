<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.showcontact" >

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hr1"
        android:label="HR"
        android:roundIcon="@drawable/circle_cropped"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShowContact">
        <activity android:name=".CreateLeaveActivity">

        </activity>
        <activity android:name=".MainLeave">

        </activity>
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Detail_Card" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>